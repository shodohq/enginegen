spec: spec.yaml
graph: graph.json
engineCase:
  synthesizer:
    type: rocket_nozzle
  geometry:
    backend:
      type: builtin.geometry.fidget
    export: ["STL"]
  pipeline:
    - type: openfoam_cfd
      mode: mock
      case_template: examples/openfoam/case_template
      stl_name: nozzle.stl
      # For real OpenFOAM runs:
      # mode: run
      # shell: "bash -lc"
      # commands:
      #   - [blockMesh]
      #   - [simpleFoam]
  analysis:
    - { type: openfoam_metrics }
  optimization:
    type: nozzle_tuner
